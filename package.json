{
  "name": "mockulus-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "pnpm workspace root for the JavaScript side of mockulus: the embedded admin UI, and later the TypeScript admin SDK.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter mockulus-ui run build",
    "dev": "pnpm --filter mockulus-ui run dev",
    "check": "pnpm --filter mockulus-ui run check",
    "test": "pnpm --filter mockulus-ui run test"
  }
}
