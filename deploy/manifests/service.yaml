# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Service
metadata:
  name: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mockulus
  ports:
    - name: mock
      port: 8080
      targetPort: mock
    - name: admin
      port: 9090
      targetPort: admin
